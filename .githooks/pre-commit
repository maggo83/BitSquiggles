#!/bin/sh
# Keep generated documentation and browser parity fixtures synchronized with Java.
set -eu

mkdir -p out
javac -d out java/bitsquiggles/*.java
java -cp out bitsquiggles.GalleryGenerator
java -cp out bitsquiggles.WebFixtureGenerator
git add docs/examples web/fixtures.json