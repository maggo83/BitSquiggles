#!/bin/sh
# Keep generated documentation and cross-language fixtures synchronized with Java.
set -eu

mkdir -p out
javac -d out java/bitsquiggles/*.java
java -cp out bitsquiggles.GalleryGenerator
java -cp out bitsquiggles.ConformanceFixtureGenerator
git add docs/examples fixtures/v1.json
